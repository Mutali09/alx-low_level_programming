#include <main.h>
/**
 * mul - Multiplies two integers
 * @x: The first integer to multiply
 * @y: The second integer to multiply with the first integer
 * Return: The outcome of multiplication between x param and y param
 */
nt mul(int x, int y)
{
	return (x * y);
}
