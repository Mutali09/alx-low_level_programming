#include "main.h"
/**
 * add - adds two numbers and returns the result
 *
 * @x: number being added
 *
 * @y: number beigng added
 *
 * Return: Always 0.
 *
 */
int add(int x, int y)
{
	return (x + y);
}
